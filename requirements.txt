# Needs Python 3.14 or newer. Each floor is the oldest release worth
# resolving to, which is not always the oldest one that imports.
requests>=2.33
# 11.3 was the first with cp314 wheels, but as of this bump it still
# carries 18 open CVEs. 12.3 is the first with none. Nothing here decodes
# an untrusted image, so this is hygiene rather than a fix.
Pillow>=12.3
# 2.12.1401 replaced pkg_resources with importlib.resources. Earlier
# releases import pkg_resources, which 3.14 no longer ships.
openvr>=2.12.1401
